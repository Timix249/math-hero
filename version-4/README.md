# Math Hero — Google Login only

Стара версія збережена. Змінено тільки Login:
- прибрані E-Mail / Passwort
- залишена тільки кнопка Google Login

У Firebase Authentication має бути увімкнений Google provider.

В `app.js` встав свій Firebase config замість `PASTE_...`.
